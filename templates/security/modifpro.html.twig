{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} : modifier profil{% endblock %}

{% block body %}

<div class="uk-card uk-card-default uk-card-body uk-width-1-3">

        {{ form_start(registrationForm, { 'attr' : { 'class' : 'uk-form-horizontal'}}) }}

            <h3 class="legend">Modifier le profil</h3>
        
            <div class="uk-margin">  
            {{ form_widget(registrationForm.email) }}
            </div>
            <div class="uk-margin">
            {{ form_widget(registrationForm.siret) }}
            </div>
            <div class="uk-margin">
            {{ form_widget(registrationForm.raisonsociale) }}
            </div>
            <div class="uk-margin">
            {{ form_widget(registrationForm.username) }}
            </div>

        <button class="uk-button uk-button-default">Enregistrement</button>

        {{ form_end(registrationForm) }}

</div>

{% endblock %}



